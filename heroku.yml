build:
  docker:
    web: Dockerfile
  config:
    METABASE_VERSION: "v0.54.9.x"  
    JAVA_TOOL_OPTIONS: "-Xmx300m"

run:
  web: bash /app/docker-entrypoint.sh

release:
  command:
    - echo "Release phase: no actions defined"
